# Compatibility shim for tools that still expect requirements.txt.
#
# Prefer one of these instead:
#   pip install -e .
#   pip install -e ".[dev]"
#
# Project dependencies are defined in pyproject.toml.
-e .
